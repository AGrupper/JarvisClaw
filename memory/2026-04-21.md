# 2026-04-21 — Morning Briefing Pipeline Audit + Cleanup

## What happened

- **Briefing pipeline audit**: walked through every component of the morning briefing pipeline — scripts, verify, garmin-connect submodule, LaunchAgent plists.
- **Fixed `verify_briefing.py`**: removed `"body battery"` and `"stress score"` from `STATE1_KEYWORDS` — these strings were never produced by the briefing composer, causing false negatives in verification.
- **Annotated `sync_garmin.py`**: added docstring to `sync_day()` specifying which sections are used by the morning briefing (`sleep`, `resting HR`, `HRV`, `training readiness`) vs. which are written to the health file but ignored by the briefing composer (`stress`, `respiration`, `fitness_age`, `intensity_minutes`, `activities`, `body battery`, `SpO2`, `weight`, `steps`, `distance`).
- **Deleted dead LaunchAgent**: removed `~/Library/LaunchAgents/com.jarvis.briefing.plist` — pointed to a non-existent script path and was silently failing every day at 05:30.
- **Things3 AppleScript timeout**: the timeout from earlier in the morning was a one-off; resolved itself. No code change needed.
- **USER.md update**: running event emoji preference added (gender-neutral 🏃 not 🏃‍♂️).

## Character

Cleanup/audit session — no new features. Three targeted fixes: one bad keyword list, one missing annotation, one dead plist.

## Pending

- garmin-connect submodule: local changes committed in submodule but not yet pushed upstream (shows as dirty). May need `cd skills/garmin-connect && git push` in a future session.

---

# 2026-04-21 — Readwise Briefing Preference

## What happened

- Amit wants Readwise added to the morning briefing.
- The Readwise section is mandatory, not optional.
- Source should be today's Readwise email only, not the past 2 days and not Notion for v1.
- The briefing should include the whole Readwise email content relevant to the daily review: all 5 highlight snippets, preserving book/source attribution when present.
- Do not compress Readwise into 1-3 bullets.

---

# 2026-04-21 — Model Routing Policy

## What happened

- Confirmed OpenClaw config already has `deepseek/deepseek-chat`, `minimax/MiniMax-M2.7`, `anthropic/claude-sonnet-4-6`, and `anthropic/claude-haiku-4-5`.
- Documented the then-current routing policy, later superseded on 2026-04-23; MiniMax M2.7 was for LLM worker/fetch subagents, Haiku was its fallback.
- Deterministic morning briefing fetches stay as parallel scripts/CLI calls because that is cheaper and faster than wrapping them in LLM subagents.
- If future briefing workers need LLM judgment for classification, extraction, filtering, or summarization, they should use MiniMax and return raw/structured data only.

## Superseded on 2026-04-23

- GPT-5.4 is now Jarvis's main orchestrator/composer via `openai-codex/gpt-5.4`.
- Fallback order is `anthropic/claude-sonnet-4-6`, then `deepseek/deepseek-chat`.
- GPT-5.4 writes final user-facing messages, including Telegram replies and morning briefings.
- GPT-5.4 Pro and GPT-5.4 Mini are not in automatic routing.
